---
id: strangler-fig-migration
name: Strangler Fig Migration
version: 1.0.0
summary: How traffic is gradually routed away from the monolith towards extracted services
---

```plantuml
@startuml
!theme plain

title Strangler Fig Migration

actor Client
participant "API Gateway\n(Routing Facade)" as Gateway
participant "Orders Service\n(extracted)" as NewOrders
participant "Core Monolith" as Monolith
database "Monolith DB" as DB

== Already migrated ==
Client -> Gateway: POST /orders
Gateway -> NewOrders: route (100%)
NewOrders --> Gateway: 201 Created
Gateway --> Client: 201 Created

== In-progress migration ==
Client -> Gateway: POST /payments
Gateway -> Monolith: route (90%)
Gateway -> NewOrders: shadow traffic (10%)
Monolith -> DB: write payment
Monolith --> Gateway: 200 OK
Gateway --> Client: 200 OK

== Not yet migrated ==
Client -> Gateway: POST /subscriptions
Gateway -> Monolith: route (100%)
Monolith -> DB: write subscription
Monolith --> Gateway: 200 OK
Gateway --> Client: 200 OK

@enduml
```

## Strangler Fig Migration

The API Gateway acts as a routing facade in front of the Core Monolith. As each
capability is extracted into its own service, the gateway shifts traffic away from
the monolith:

1. **Orders** is fully migrated — 100% of traffic hits the extracted service.
2. **Payments** is mid-migration — most traffic still hits the monolith, with a
   small percentage shadowed to the new implementation for comparison.
3. **Subscriptions** has not started migrating — all traffic still flows to the
   monolith.

This is the mechanism by which the Core Monolith shrinks over time.

---
id: core-monolith
name: Core Monolith
version: 1.0.0
summary: The legacy core monolith that FlowMart is gradually decomposing into fine-grained services. A system groups the services that make up this larger transitional codebase, independently of which domains those services belong to.
owners:
  - dboyne
services:
  - id: OrdersService
  - id: PaymentService
  - id: SubscriptionService
flows:
  - id: PlaceOrderFlow
    version: 1.0.0
  - id: PaymentFlow
    version: 1.0.0
  - id: CancelSubscription
    version: 1.0.0
entities:
  - id: Order
  - id: Payment
  - id: BillingProfile
containers:
  - id: monolith-db
  - id: monolith-session-cache
  - id: orders-db
  - id: payments-db
diagrams:
  - id: monolith-module-map
    version: 1.0.0
  - id: strangler-fig-migration
    version: 1.0.0
  - id: target-architecture
    version: 1.0.0
---

## Overview

The **Core Monolith** is FlowMart's original application. As part of our
migration to microservices, the services that we carve out of the monolith are
grouped under this system so the team can see, at a glance, which parts of the
catalog still belong to the monolith during the transition.

<NodeGraph />

Systems are **orthogonal to domains** — a single system can contain services
from multiple domains, and a domain can span multiple systems.

## Services in this system

The Core Monolith currently groups the following services, which span several
domains:

- <ResourceLink id="OrdersService" type="service">Orders Service</ResourceLink> — order management (Orders domain)
- <ResourceLink id="PaymentService" type="service">Payment Service</ResourceLink> — payment processing (Payment domain)
- <ResourceLink id="SubscriptionService" type="service">Subscription Service</ResourceLink> — subscriptions (Subscriptions domain)

Notice these services come from [[service|OrdersService]], [[service|PaymentService]]
and [[service|SubscriptionService]] across different domains — that's the whole
point of a system: it groups services regardless of their domain.

## Migration status

We are incrementally extracting bounded contexts out of the monolith. Each
extracted service keeps its domain membership while remaining part of this
system until the migration completes.

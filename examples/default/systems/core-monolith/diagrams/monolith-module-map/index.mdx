---
id: monolith-module-map
name: Monolith Module Map
version: 1.0.0
summary: C4-style component view of the modules still living inside the Core Monolith
---

```plantuml
@startuml
!theme plain

title Core Monolith - Module Map

package "Core Monolith" {
  [Orders Module] as Orders
  [Payments Module] as Payments
  [Subscriptions Module] as Subs
  [Billing Module] as Billing
  [Notifications Module] as Notify
}

database "Monolith DB\n(PostgreSQL)" as DB
queue "Legacy Job Queue" as Queue

Orders --> DB
Payments --> DB
Subs --> DB
Billing --> DB

Orders --> Queue
Payments --> Queue
Notify <-- Queue

Subs --> Billing : creates invoices
Payments --> Billing : settles invoices

@enduml
```

## Monolith Module Map

This diagram shows the modules that still live inside the **Core Monolith** and how
they share state through a single database. As part of the strangler-fig migration,
each of these modules is gradually being carved out into its own service.

- **Orders / Payments / Subscriptions** are the first candidates for extraction.
- **Billing** and **Notifications** remain tightly coupled to the shared schema.
- The shared **Monolith DB** is the biggest blocker to full decomposition.

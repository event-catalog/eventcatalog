---
'@eventcatalog/core': minor
---

Show the EventCatalog trial in the header while running `eventcatalog dev`. The badge counts down the 90-day trial from the catalog's trial start date (`tsd` in `eventcatalog.config.js`), shows "Trial ended" once it's over, and links to the license FAQ. It never appears in built or served catalogs.

---
'@eventcatalog/core': major
---

Remove the Domain Integration Map page (`/visualiser/domain-integrations`), which wasn't linked from anywhere; domain diagrams (L1) show how domains integrate. Entity diagram pages and custom docs domain embeds now say "Diagram" instead of "Map", matching the sidebar.

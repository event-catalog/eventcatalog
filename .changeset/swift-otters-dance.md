---
"@eventcatalog/core": minor
---

Add settings UI, refresh homepage and theme styling, introduce `chat.enabled` config flag, and add new `--ec-rail-bg` / `--ec-rail-active-bg` CSS variables for sidebar rail theming.

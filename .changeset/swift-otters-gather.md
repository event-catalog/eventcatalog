---
"@eventcatalog/core": patch
---

Add systems support to AI Chat, catalog tools, and sidebar navigation. The chat panel now recognises system pages and resources, the AI tools accept the `systems` collection, the custom documentation page renders the System Context Map when a domain has systems, and the System section in the sidebar shows the System Context Map when systems exist.

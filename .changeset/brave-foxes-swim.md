---
"@eventcatalog/cli": patch
"@eventcatalog/language-server": patch
"@eventcatalog/sdk": patch
"@eventcatalog/dsl-playground": patch
---

Improve DSL import with container ref support, resource stubs, and cache upsert; add writes-to/reads-from compilation in language server; enhance playground with export and zip download feature

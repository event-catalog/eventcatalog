---
"@eventcatalog/core": patch
---

Replace the resource-specific Browse sidebar links with a single Discover link and add a Kind filter to discover pages for switching between available resource collections.

---
'@eventcatalog/core': patch
'@eventcatalog/create-eventcatalog': patch
---

feat(core): add `tsd` (trial start date) to `eventcatalog.config.js` on new catalogs and on dev/build for existing catalogs, and include it in create, dev and build analytics events

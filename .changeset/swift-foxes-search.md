---
"@eventcatalog/core": minor
"@eventcatalog/create-eventcatalog": minor
---

Add opt-in indexed full-content search powered by Pagefind. Enable by setting `search: { type: 'indexed' }` in `eventcatalog.config.js` to index markdown content across resources, custom docs, resource docs and changelogs. Resource search remains the default.

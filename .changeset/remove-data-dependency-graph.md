---
'@eventcatalog/core': patch
---

Remove the service "Data Dependency Graph" (`/visualiser/services/:id/:version/data`). Level 2 of the service's Diagram shows the same data stores (and the services it talks to), so the page now redirects there and the sidebar link is gone.

---
"@eventcatalog/core": patch
---

Only show the EventCatalog update banner when a newer published version exists. A stale update check no longer suggests downgrading (for example 4.11.0 → 4.10.10).

---
'@eventcatalog/core': patch
'@eventcatalog/linter': patch
'@eventcatalog/create-eventcatalog': patch
---

Fix llms.txt list formatting and hyphenated ubiquitous-language URLs, load nested `domains/{parent}/domains/{child}` catalogs, and replace leftover Next.js create-catalog flags with `--skills`/`--no-skills` plus template validation.

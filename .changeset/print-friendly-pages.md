---
"@eventcatalog/core": patch
---

Add export to PDF for message pages (events, commands, queries) with print-friendly layout, schema viewer, producer/consumer tables, version history, and owner details

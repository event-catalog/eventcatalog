---
"@eventcatalog/core": patch
"@eventcatalog/create-eventcatalog": patch
---

Bundle EventCatalog docs into `@eventcatalog/core` so coding agents can read the source-of-truth documentation locally, and scaffold `AGENTS.md` / `CLAUDE.md` into new projects so agents are pointed at those bundled docs before making changes.
